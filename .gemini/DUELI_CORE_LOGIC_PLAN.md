# 🎯 خطة إكمال المنطق الأساسي لمنصة ديولي
# Dueli Core Logic Completion Plan

> **الهدف:** إكمال كل المنطق المفقود قبل الإطلاق – ليس التحسين، بل البناء الأساسي.

---

## 📌 الجزء الأول: دورة حياة المنافسة الكاملة (Competition Lifecycle)

### 1.1 الحالات والانتقالات

```
[إنشاء] → pending (لاحقة فورية أو مجدولة، بانتظار خصم)
    ↓ (قبول خصم)
[قبول] → accepted (الطرفان جاهزان، بانتظار البث)
    ↓ (بدء البث من الطرفين)
[بث] → live (حية، الطرفان يبثان)
    ↓ (إنهاء أو انتهاء الوقت)
[انتهاء] → completed (مسجلة، متاحة للمشاهدة)

[إلغاء من أي نقطة قبل live] → cancelled → حذف
```

### 1.2 نوعان من المنافسات اللاحقة

| | الفورية (Immediate) | المجدولة (Scheduled) |
|---|---|---|
| **وقت البدء** | لحظة قبول الخصم | الوقت المسجل عند الإنشاء |
| **المهلة قبل الحذف** | ساعة من لحظة القبول | ساعة من الوقت المجدول |
| **التحول لحية** | فور بدء الطرفين البث | فور بدء الطرفين البث + وصول الوقت المجدول |
| **مثال** | "عايز منافس دلوقتي" | "منافسة يوم الجمعة 8 مساءً" |

### 1.3 قاعدة الساعة (المهلة)

**للفورية:** تبدأ الساعة من لحظة قبول الخصم. إذا لم يبدأ الطرفان البث خلال ساعة → حذف.

**للمجدولة:** تبدأ الساعة من الوقت المجدول. إذا لم يبدأ الطرفان البث خلال ساعة من الموعد → حذف.

**في كلتا الحالتين:** إذا تحولت لحية (بدأ الطرفان البث) تنجو من الحذف.

### 1.4 الحد الأقصى للمنافسة الحية

- **الحد:** ساعتان من لحظة التحول لـ `live`
- **عند الانتهاء:** تتحول تلقائياً لـ `completed` (مسجلة)
- **يظهر عداد تنازلي** في صفحة المنافسة
- **يتم إعلام الجميع** (متنافسين + مشاهدين) وتحويلهم للرئيسية

---

## 📌 الجزء الثاني: المخاطر والحلول (Edge Cases)

### خطر 1: مشاركة متنافس في أكثر من منافسة حية ⚔️

**الحل: حالة "مشغول" (Busy Status)**

```
عندما يضغط المتنافس "بدء البث":
├── تتحول حالته إلى busy = true
├── لا يمكنه بدء بث آخر
├── تظهر حالة "مشغول" في:
│   ├── نتائج البحث عنه
│   ├── صفحة حسابه
│   ├── الدعوات المقدمة منه
│   └── محاولات الآخرين لدعوته
│
├── إذا جاء موعد منافسة أخرى وهو مشغول:
│   ├── يظهر إشعار: "عندك منافسة تانية بدأ وقتها"
│   ├── خيار 1: إنهاء البث الحالي → تحويل تلقائي للمنافسة الأخرى
│   │   ├── المنافسة الحالية إن كانت حية → تتحول لمسجلة
│   │   └── المنافسة الحالية إن كانت لاحقة (الطرف الآخر لم يبث) → تُحذف
│   └── خيار 2: رفض → تُحذف المنافسة الأخرى + اعتذار للطرف الآخر
│
└── يتحرر من "مشغول" فقط عند:
    ├── إنهاء البث يدوياً
    ├── انتهاء وقت المنافسة (ساعتان)
    └── إنهاء الطرف الآخر للمنافسة
```

**التنفيذ:**
```sql
-- إضافة حقل في جدول users
ALTER TABLE users ADD COLUMN is_busy BOOLEAN DEFAULT 0;
ALTER TABLE users ADD COLUMN active_competition_id INTEGER REFERENCES competitions(id);
```

```typescript
// في CompetitionController - بدء البث
async startStreaming(c: AppContext) {
    const user = this.getCurrentUser(c);
    
    // تحقق: هل المستخدم مشغول؟
    if (user.is_busy) {
        return this.error(c, 'أنت في بث حالياً. أنهِ البث الحالي أولاً.', 409);
    }
    
    // تحويل لمشغول
    await userModel.update(user.id, { 
        is_busy: true, 
        active_competition_id: competitionId 
    });
    
    // ... بدء البث
}
```

---

### خطر 2: هجر المنافسات المُنشأة 🏚️

**الحل: قاعدة الساعة + التنظيف الدوري**

```
المنافسة pending (بدون خصم):
├── الفورية: تُحذف بعد ساعة من الإنشاء إن لم ينضم خصم
└── المجدولة: تُحذف بعد ساعة من الموعد المجدول إن لم ينضم خصم

المنافسة accepted (بخصم، لم تبدأ):
├── الفورية: تُحذف بعد ساعة من لحظة القبول
└── المجدولة: تُحذف بعد ساعة من الموعد المجدول
```

**دالة السيرفر المؤقتة:**
```typescript
// Cron كل 5 دقائق
async function cleanupExpiredCompetitions(db: D1Database) {
    // 1. فورية pending بدون خصم - مر ساعة على إنشائها
    await db.prepare(`
        DELETE FROM competitions 
        WHERE status = 'pending' 
        AND scheduled_at IS NULL
        AND opponent_id IS NULL
        AND created_at < datetime('now', '-1 hour')
    `).run();
    
    // 2. فورية accepted - مر ساعة على القبول
    await db.prepare(`
        UPDATE competitions SET status = 'cancelled'
        WHERE status = 'accepted' 
        AND scheduled_at IS NULL
        AND accepted_at < datetime('now', '-1 hour')
    `).run();
    
    // 3. مجدولة - مر ساعة على الموعد المجدول ولم تتحول لحية
    await db.prepare(`
        UPDATE competitions SET status = 'cancelled'
        WHERE status IN ('pending', 'accepted')
        AND scheduled_at IS NOT NULL
        AND scheduled_at < datetime('now', '-1 hour')
    `).run();
    
    // 4. حية - مر ساعتان على بدئها
    const expired = await db.prepare(`
        SELECT id FROM competitions 
        WHERE status = 'live'
        AND started_at < datetime('now', '-2 hours')
    `).all();
    
    for (const comp of expired.results) {
        await forceCompleteCompetition(db, comp.id);
    }
}
```

---

### خطر 3: انقطاع أحد المتنافسين أثناء البث 📡

**الحل:**
```
عند انقطاع أحد المتنافسين:
├── ينتظر 3 دقائق (فترة سماح)
├── يظهر للمشاهدين: "المتنافس X انقطع، ننتظر عودته..."
│
├── إذا عاد خلال 3 دقائق → يستمر البث عادي
└── إذا لم يعد بعد 3 دقائق:
    ├── يُسأل الطرف الآخر: "هل تريد إنهاء المنافسة؟"
    ├── نعم → تتحول لمسجلة
    └── لا → ينتظر 3 دقائق إضافية ثم تنتهي تلقائياً
```

---

### خطر 4: تضارب الطلبات 🔀

**قاعدة صارمة: المنافسة = طرفان فقط**

```typescript
async acceptRequest(c: AppContext) {
    const { competitionId, requesterId } = await c.req.json();
    
    // تحقق: هل المنافسة لا تزال بحاجة لخصم؟
    const comp = await competitionModel.findById(competitionId);
    if (comp.opponent_id !== null) {
        return this.error(c, 'المنافسة لديها خصم بالفعل', 409);
    }
    
    // تحقق: هل المنشئ هو من يقبل؟
    if (comp.creator_id !== user.id) {
        return this.error(c, 'فقط منشئ المنافسة يمكنه قبول الطلبات', 403);
    }
    
    // ✅ قبول الطلب + تعيين الخصم (عملية ذرية)
    await db.batch([
        // 1. تعيين الخصم
        db.prepare(`
            UPDATE competitions 
            SET opponent_id = ?, status = 'accepted', accepted_at = datetime('now')
            WHERE id = ? AND opponent_id IS NULL
        `).bind(requesterId, competitionId),
        
        // 2. حذف كل الطلبات الأخرى لهذه المنافسة
        db.prepare(`
            DELETE FROM competition_requests WHERE competition_id = ?
        `).bind(competitionId),
        
        // 3. إشعار الطلبات المرفوضة
        db.prepare(`
            INSERT INTO notifications (user_id, type, title, message, reference_type, reference_id)
            SELECT requester_id, 'request_declined', 'تم اختيار متنافس آخر', 
                   'تم قبول متنافس آخر في المنافسة', 'competition', ?
            FROM competition_requests 
            WHERE competition_id = ? AND requester_id != ?
        `).bind(competitionId, competitionId, requesterId),
    ]);
    
    // 4. إشعار المقبول
    await notificationModel.create({
        user_id: requesterId,
        type: 'request_accepted',
        title: 'تم قبول طلبك!',
        message: `تم قبولك كمنافس في: ${comp.title}`,
        reference_type: 'competition',
        reference_id: competitionId
    });
}
```

**القاعدة الحاسمة:** بعد القبول، لا يمكن تغيير أي من الطرفين أبداً.

---

### خطر 5: بقاء الطلبات معلقة للأبد 📨

**الحل: الطلبات تحيا وتموت مع المنافسة**

```
الطلب يُحذف تلقائياً عند:
├── حذف المنافسة المرتبطة
├── قبول طلب آخر لنفس المنافسة
├── قبول دعوة من المنشئ لمستخدم آخر
├── حذف يدوي من المرسل أو المستقبل
└── إلغاء المنافسة

قبل حذف أي منافسة:
├── 1. حذف كل طلباتها
├── 2. حذف كل إشعاراتها
└── 3. ثم حذف المنافسة نفسها
```

```typescript
async deleteCompetition(db: D1Database, competitionId: number) {
    await db.batch([
        db.prepare('DELETE FROM competition_requests WHERE competition_id = ?').bind(competitionId),
        db.prepare('DELETE FROM notifications WHERE reference_type = ? AND reference_id = ?')
          .bind('competition', competitionId),
        db.prepare('DELETE FROM competitions WHERE id = ?').bind(competitionId),
    ]);
}
```

---

### خطر 6: بقاء الإشعارات معلقة للأبد 🔔

**الحل: دالة سيرفر شهرية**

```typescript
// Cron أول كل شهر
async function cleanupOldNotifications(db: D1Database) {
    await db.prepare(`
        DELETE FROM notifications 
        WHERE created_at < datetime('now', '-30 days')
    `).run();
}
```

---

### خطر إضافي 7: حذف المستخدمين غير النشطين 👻

**دالة سنوية:**
```typescript
async function cleanupInactiveUsers(db: D1Database) {
    // المستخدم غير النشط = لم يُنشئ ولم يشارك في أي منافسة + غائب سنة
    const inactiveUsers = await db.prepare(`
        SELECT u.id FROM users u
        WHERE u.last_login_at < datetime('now', '-1 year')
        AND NOT EXISTS (SELECT 1 FROM competitions c WHERE c.creator_id = u.id)
        AND NOT EXISTS (SELECT 1 FROM competitions c WHERE c.opponent_id = u.id)
    `).all();
    
    for (const user of inactiveUsers.results) {
        await db.batch([
            db.prepare('DELETE FROM notifications WHERE user_id = ?').bind(user.id),
            db.prepare('DELETE FROM follows WHERE follower_id = ? OR following_id = ?').bind(user.id, user.id),
            db.prepare('DELETE FROM comments WHERE user_id = ?').bind(user.id),
            db.prepare('DELETE FROM ratings WHERE user_id = ?').bind(user.id),
            db.prepare('DELETE FROM likes WHERE user_id = ?').bind(user.id),
            db.prepare('DELETE FROM sessions WHERE user_id = ?').bind(user.id),
            db.prepare('DELETE FROM user_settings WHERE user_id = ?').bind(user.id),
            db.prepare('DELETE FROM users WHERE id = ?').bind(user.id),
        ]);
    }
}
```

---

### خطر إضافي 8: الحظر بين المستخدمين 🚫

```sql
CREATE TABLE user_blocks (
    blocker_id INTEGER NOT NULL REFERENCES users(id),
    blocked_id INTEGER NOT NULL REFERENCES users(id),
    created_at DATETIME DEFAULT (datetime('now')),
    PRIMARY KEY (blocker_id, blocked_id)
);
```

**تأثير الحظر:**
- لا يمكن للمحظور إرسال طلبات أو دعوات
- لا يمكنه الانضمام لمنافسات الحاظر
- **يمكنه** مشاهدة المنافسات والتعليق عليها

---

### خطر إضافي 9: حذف المنافسات – من يحق له؟

```
قواعد الحذف:
├── المنشئ يمكنه حذف منافسة أنشأها فقط إذا كانت pending أو accepted
├── لا يمكن لأي طرف حذف منافسة حية (live) أو مسجلة (completed)
├── المنافسات المسجلة (completed) لا تُحذف أبداً بشرياً
└── فقط نظام التنظيف التلقائي أو الإدارة يمكنهم حذف المسجلة
```

---

### خطر إضافي 10: البث – Missing Chunks + التنظيف

```
1. Retry Queue: لا ننتقل للقطعة 5 إلا بتأكيد وصول 4
2. بعد الدمج في MP4 → حذف كل القطع فوراً
3. السيرفر لا يقبل رفع إلا من نطاق المنصة (Origin check)
```

---

## 📌 الجزء الثالث: نظام العرض والمقترحات (Recommendation Engine)

### 3.1 معايير الترتيب (11 معيار)

| # | المعيار | الوزن | التفاصيل |
|---|---------|-------|----------|
| 1 | لغة المستخدم + لغة بلده | عالي | منافسات بنفس اللغة أولاً |
| 2 | بلد المستخدم | عالي | منافسات من نفس البلد |
| 3 | الأحدث | متوسط | الأحدث إنشاءً |
| 4 | الأعلى مشاهدة | متوسط | total_views |
| 5 | الأعلى تقييماً | متوسط | average_rating |
| 6 | متنافسون يتابعهم | عالي | من جدول follows |
| 7 | أقسام فرعية يشاهدها كثيراً | عالي | من سجل المشاهدة |
| 8 | منافسات أعجب بها | متوسط | من جدول likes |
| 9 | مشابهة لما أعجبه | متوسط | نفس القسم/اللغة/البلد |
| 10 | مشابهة لما شاهده | متوسط | كلمات مفتاحية |
| 11 | **استبعاد ما شاهده** | حاسم | لا تُعاد إلا في سجل المشاهدة |

**القاعدة:** الترتيب **عشوائي** داخل كل صف/مجموعة لضمان التنوع.

### 3.2 الجداول المطلوبة

```sql
-- سجل المشاهدة
CREATE TABLE watch_history (
    user_id INTEGER NOT NULL REFERENCES users(id),
    competition_id INTEGER NOT NULL REFERENCES competitions(id),
    watched_at DATETIME DEFAULT (datetime('now')),
    watch_duration_seconds INTEGER DEFAULT 0,
    PRIMARY KEY (user_id, competition_id)
);

-- كلمات مفتاحية المستخدم (تُبنى تلقائياً من عناوين ما يشاهده)
CREATE TABLE user_keywords (
    user_id INTEGER NOT NULL REFERENCES users(id),
    keyword TEXT NOT NULL,
    weight REAL DEFAULT 1.0,
    updated_at DATETIME DEFAULT (datetime('now')),
    PRIMARY KEY (user_id, keyword)
);

-- قائمة المشاهدة اللاحقة (Watch Later)
CREATE TABLE watch_later (
    user_id INTEGER NOT NULL REFERENCES users(id),
    competition_id INTEGER NOT NULL REFERENCES competitions(id),
    added_at DATETIME DEFAULT (datetime('now')),
    PRIMARY KEY (user_id, competition_id)
);
```

### 3.3 سجلات المستخدم (4 سجلات)

| السجل | المصدر | العمليات |
|-------|--------|---------|
| **سجل المشاهدة** | watch_history | بحث، حذف مفردة، عرض |
| **قائمة المشاهدة اللاحقة** | watch_later | إضافة، حذف، عرض |
| **الإعجابات** | likes | إضافة، إزالة، عرض |
| **المتابعات** | follows | متابعة، إلغاء، عرض |

### 3.4 المنافسات المشابهة في كل صفحة

```typescript
async getRelatedCompetitions(competitionId: number, limit = 10) {
    const comp = await this.findById(competitionId);
    
    return await this.db.prepare(`
        SELECT c.*, u1.display_name as creator_name, u2.display_name as opponent_name
        FROM competitions c
        LEFT JOIN users u1 ON c.creator_id = u1.id
        LEFT JOIN users u2 ON c.opponent_id = u2.id
        WHERE c.id != ?
        AND c.status IN ('live', 'completed')
        AND (c.subcategory_id = ? OR c.category_id = ? OR c.language = ?)
        ORDER BY 
            CASE WHEN c.subcategory_id = ? THEN 3 ELSE 0 END +
            CASE WHEN c.category_id = ? THEN 2 ELSE 0 END +
            CASE WHEN c.language = ? THEN 1 ELSE 0 END DESC,
            RANDOM()
        LIMIT ?
    `).bind(competitionId, comp.subcategory_id, comp.category_id, 
            comp.language, comp.subcategory_id, comp.category_id, 
            comp.language, limit).all();
}
```

---

## 📌 الجزء الرابع: رأيي في حلولك المقترحة

| # | الاقتراح | الحكم | التعليق |
|---|----------|-------|---------|
| 1 | حذف إشعارات أقدم من شهر | ✅ ممتاز | أضف: اجعلها 30 يوم بالضبط، واحتفظ بالإشعارات غير المقروءة 60 يوم |
| 2 | حذف مستخدمين غير نشطين سنة | ✅ ممتاز | أضف: أرسل تحذير بالبريد قبل الحذف بأسبوع |
| 3 | حذف طلبات وإشعارات المنافسة قبل حذفها | ✅ حاسم | هذا cascade delete ضروري، نفذه كـ batch |
| 4 | الطلبات مرتبطة بالمنافسة | ✅ ممتاز | صحيح 100% – الطلب يموت مع المنافسة أو عند قبول غيره |
| 5 | لا تغيير للطرفين بعد القبول | ✅ حاسم | أضف: حتى الإدارة لا تغير (حماية قانونية) |
| 6 | حذف يدوي للطلبات + حظر | ✅ ممتاز | الحظر يمنع الطلبات فقط لا المشاهدة – منطقي |
| 7 | حذف اللاحقة بعد ساعة | ✅ ممتاز | التفريق بين الفورية والمجدولة سليم، أضف: عداد تنازلي مرئي |
| 8 | حد ساعتين للحية | ✅ ممتاز | أضف: تحذير عند 10 دقائق و1 دقيقة قبل الانتهاء |
| 9 | حالة مشغول | ✅ حاسم وذكي | المنطق متكامل، أضف: زمن timeout للـ busy إذا انقطع الاتصال |
| - | Retry Queue للقطع | ✅ ضروري | لا ننتقل للقطعة التالية بدون تأكيد |
| - | حذف القطع بعد الدمج | ✅ ضروري | فوراً بعد التأكد من سلامة MP4 |
| - | رفض رفع من خارج المنصة | ✅ أمني | Origin + Referer check + session token |

### ⚠️ ما ينقص ويجب إضافته:

| # | الخطر المفقود | الحل |
|---|-------------|------|
| 1 | **timeout للـ busy**: لو انقطع نت المتنافس نهائياً، يبقى busy للأبد | دالة كل 10 دقائق: إذا كان busy ومنافسته ليست live → حرره |
| 2 | **حماية من الإنشاء المفرط**: مستخدم ينشئ 100 منافسة | حد 3 منافسات pending كحد أقصى لكل مستخدم |
| 3 | **حماية من الطلبات المفرطة**: مستخدم يرسل 100 طلب | حد 10 طلبات معلقة كحد أقصى لكل مستخدم |
| 4 | **تضارب زمني**: مستخدم يقبل منافستين في نفس الوقت | قبل القبول: تحقق من عدم وجود منافسة accepted في نفس الفترة |
| 5 | **المتفرج يصبح منافساً**: وهو يشاهد منافسة، يُقبل في أخرى | إشعار فقط، لا تحويل تلقائي |
| 6 | **البحث عن منافس أثناء الإنشاء**: المنشئ يبحث عن متنافسين | عرض قائمة متنافسين متاحين في نفس القسم + زر "أرسل دعوة" |

---

## 📌 الجزء الخامس: خطة التنفيذ المفصلة (12 أسبوع)

### المرحلة 1: البنية التحتية (الأسبوع 1-2) 🏗️

| # | المهمة | الملفات | ساعات | المسؤول |
|---|--------|---------|:-----:|---------|
| 1.1 | إنشاء schema.sql كامل | `schema.sql` (جديد) | 8 | Agent-Backend |
| 1.2 | إضافة الجداول الجديدة (watch_history, user_keywords, watch_later, user_blocks) | `schema.sql` | 3 | Agent-Backend |
| 1.3 | إضافة حقول (is_busy, active_competition_id, accepted_at) | `schema.sql` + migration | 2 | Agent-Backend |
| 1.4 | توحيد CompetitionStatus type + تحديث seed.sql | `types.ts`, `seed.sql` | 2 | Agent-Backend |
| 1.5 | إنشاء Cron Handler في wrangler.jsonc + main.ts | `wrangler.jsonc`, `main.ts` | 3 | Agent-Backend |
| 1.6 | إنشاء error classes + error-handler middleware | `middleware/` (جديد) | 3 | Agent-Backend |

**اختبار المرحلة 1:**
```
□ wrangler d1 execute --file=schema.sql يعمل بدون أخطاء
□ npm run dev يعمل بنجاح
□ الـ Cron يُسجل في Cloudflare dashboard
```

---

### المرحلة 2: منطق المنافسات الأساسي (الأسبوع 3-4) ⚔️

| # | المهمة | الملفات | ساعات | المسؤول |
|---|--------|---------|:-----:|---------|
| 2.1 | منطق الإنشاء: فورية vs مجدولة + حد 3 pending | `CompetitionController.ts` | 6 | Agent-Backend |
| 2.2 | منطق الطلبات: إرسال/قبول/رفض/حذف + حد 10 طلبات | `CompetitionController.ts` | 8 | Agent-Backend |
| 2.3 | منطق الدعوات: المنشئ يدعو + قبول/رفض | `CompetitionController.ts` | 6 | Agent-Backend |
| 2.4 | قبول الخصم: حذف الطلبات الأخرى + إشعارات | `CompetitionController.ts` | 4 | Agent-Backend |
| 2.5 | حماية التضارب الزمني | `CompetitionModel.ts` | 4 | Agent-Backend |
| 2.6 | حالة مشغول: تفعيل/تحرير/timeout | `UserModel.ts`, `CompetitionController.ts` | 6 | Agent-Backend |
| 2.7 | Cascade Delete: منافسة → طلبات → إشعارات | `CompetitionModel.ts` | 3 | Agent-Backend |
| 2.8 | لا تغيير للطرفين بعد القبول | `CompetitionController.ts` | 2 | Agent-Backend |

**اختبار المرحلة 2:**
```
□ إنشاء منافسة فورية ← تظهر في القائمة
□ إنشاء 4 منافسات ← الرابعة تُرفض
□ إرسال طلب انضمام ← يظهر للمنشئ
□ قبول طلب ← الطلبات الأخرى تُحذف
□ محاولة تغيير الخصم بعد القبول ← تُرفض
□ منافسة مع تضارب زمني ← تُرفض
```

---

### المرحلة 3: المؤقتات والتنظيف التلقائي (الأسبوع 5-6) ⏰

| # | المهمة | الملفات | ساعات | المسؤول |
|---|--------|---------|:-----:|---------|
| 3.1 | Cron كل 5 دقائق: حذف المنافسات المنتهية | `ScheduleController.ts` | 6 | Agent-Backend |
| 3.2 | Cron شهري: حذف إشعارات > 30 يوم | `ScheduleController.ts` | 2 | Agent-Backend |
| 3.3 | Cron سنوي: حذف مستخدمين غير نشطين | `ScheduleController.ts` | 4 | Agent-Backend |
| 3.4 | حد ساعتين للمنافسة الحية + إنهاء تلقائي | `ScheduleController.ts` | 4 | Agent-Backend |
| 3.5 | تحرير busy عند timeout (10 دقائق بدون منافسة حية) | `ScheduleController.ts` | 3 | Agent-Backend |
| 3.6 | عداد تنازلي في واجهة المنافسة اللاحقة والحية | `competition-page.ts`, `live-room-page.ts` | 6 | Agent-Frontend |
| 3.7 | تحويل تلقائي للرئيسية عند انتهاء/حذف المنافسة | `live-room-page.ts` | 3 | Agent-Frontend |

**اختبار المرحلة 3:**
```
□ منافسة فورية بدون خصم ← تُحذف بعد ساعة
□ منافسة مجدولة تخطت موعدها ← تُحذف بعد ساعة
□ منافسة حية ← تنتهي بعد ساعتين
□ مستخدم مشغول بدون منافسة حية ← يتحرر بعد 10 دقائق
□ العداد التنازلي يظهر بشكل صحيح
```

---

### المرحلة 4: الإشعارات الفورية والحظر (الأسبوع 7-8) 🔔

| # | المهمة | الملفات | ساعات | المسؤول |
|---|--------|---------|:-----:|---------|
| 4.1 | SSE endpoint للإشعارات الفورية | `api/notifications/` | 8 | Agent-Backend |
| 4.2 | Client-side SSE listener + Toast UI | `NotificationService.ts` (جديد) | 6 | Agent-Frontend |
| 4.3 | إشعار "أنت مشغول" مع خيارين | `live-room-page.ts` | 4 | Agent-Frontend |
| 4.4 | نظام الحظر: جدول + API + تحقق | `UserController.ts`, `user_blocks` | 6 | Agent-Backend |
| 4.5 | عرض حالة "مشغول" في البحث والملف الشخصي | `profile-page.ts`, `explore-page.ts` | 4 | Agent-Frontend |

**اختبار المرحلة 4:**
```
□ إشعار يظهر فوراً عند قبول طلب (بدون تحديث الصفحة)
□ حظر مستخدم ← لا يمكنه إرسال طلبات
□ المحظور يمكنه المشاهدة والتعليق
□ حالة "مشغول" تظهر في الملف الشخصي
```

---

### المرحلة 5: نظام العرض والمقترحات (الأسبوع 9-10) 🎯

| # | المهمة | الملفات | ساعات | المسؤول |
|---|--------|---------|:-----:|---------|
| 5.1 | تسجيل المشاهدة + استخراج كلمات مفتاحية | `WatchHistoryModel.ts` (جديد) | 6 | Agent-Backend |
| 5.2 | API المقترحات (11 معيار) | `RecommendationController.ts` (جديد) | 10 | Agent-Backend |
| 5.3 | صفحة سجل المشاهدة + بحث + حذف | `watch-history-page.ts` (جديد) | 6 | Agent-Frontend |
| 5.4 | قائمة "شاهد لاحقاً" | `WatchLaterModel.ts` (جديد) | 4 | Agent-Backend |
| 5.5 | المنافسات المشابهة في صفحة كل منافسة | `competition-page.ts` | 4 | Agent-Frontend |
| 5.6 | استبعاد ما شاهده من المقترحات | `RecommendationController.ts` | 3 | Agent-Backend |
| 5.7 | ترتيب عشوائي داخل كل صف | `RecommendationController.ts` | 2 | Agent-Backend |

---

### المرحلة 6: البث والتسجيل (الأسبوع 11-12) 📡

| # | المهمة | الملفات | ساعات | المسؤول |
|---|--------|---------|:-----:|---------|
| 6.1 | Retry Queue للقطع المفقودة | `ChunkUploader.ts` | 6 | Agent-Frontend |
| 6.2 | حذف القطع بعد دمج MP4 | `VideoCompositor.ts` | 3 | Agent-Frontend |
| 6.3 | Origin check على سيرفر البث | `signaling/routes.ts` | 3 | Agent-Backend |
| 6.4 | منطق الانقطاع (3 دقائق سماح) | `P2PConnection.ts` | 6 | Agent-Frontend |
| 6.5 | ربط البث بحالة المنافسة (pending→accepted→live→completed) | `CompetitionController.ts`, `live-room-page.ts` | 8 | Agent-Full |
| 6.6 | المنافسة المجدولة: لا تتحول لحية إلا عند الموعد | `CompetitionController.ts` | 4 | Agent-Backend |

---

## 📌 الجزء السادس: آلية التحقق والاختبار

### لكل مرحلة، يجب:

```
1. Agent ينفذ المهام
2. Agent يكتب اختبارات لكل مهمة
3. Agent يشغّل npm run dev ويتحقق من عدم وجود أخطاء build
4. Agent ينفذ الاختبارات ويؤكد نجاحها
5. Agent يعرض ملخص: ماذا فعل + ماذا لم يفعل + ماذا يحتاج مراجعة
```

### قائمة الاختبار الشاملة النهائية:

```
السيناريو 1: دورة حياة منافسة فورية كاملة
□ إنشاء → ظهور → طلب انضمام → قبول → بدء بث → حية → إنهاء → مسجلة

السيناريو 2: منافسة مجدولة
□ إنشاء → ظهور → قبول → انتظار الموعد → بدء بث → حية → مسجلة

السيناريو 3: الحذف التلقائي
□ فورية بدون خصم بعد ساعة → محذوفة
□ مجدولة تخطت موعدها بساعة → محذوفة
□ حية تخطت ساعتين → مسجلة

السيناريو 4: التضارب
□ مستخدم مشغول يحاول البث → مرفوض
□ منافستان في نفس الوقت → الثانية مرفوضة
□ قبول طلبين لنفس المنافسة → الثاني مرفوض

السيناريو 5: الإشعارات
□ إشعار فوري عند قبول طلب (بدون تحديث)
□ إشعار "أنت مشغول" مع خيارين
□ إشعارات > 30 يوم محذوفة
```

---

## 📊 ملخص الأرقام

| المقياس | القيمة |
|---------|--------|
| إجمالي المهام | **42 مهمة** |
| إجمالي الساعات المقدرة | **~190 ساعة** |
| ملفات جديدة | **~12 ملف** |
| ملفات تعديل | **~15 ملف** |
| جداول جديدة | **4 جداول** |
| Cron jobs | **3 (كل 5 دقائق، شهري، سنوي)** |
| مراحل التسليم | **6 مراحل × أسبوعين** |

---

*"المشروع فكرته ممتازة ومنطقك سليم 100%. المقترحات التسعة كلها عملية ومدروسة. ما أضفته هو 6 مخاطر إضافية + تفاصيل التنفيذ. ابدأ بالمرحلة 1 (البنية) ثم 2 (منطق المنافسات) والباقي يتبع."*
