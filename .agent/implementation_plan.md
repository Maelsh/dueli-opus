# خطة تنفيذ شاملة - منصة Dueli
> **الموعد النهائي**: 28-31 ديسمبر 2024 (أسبوع إلى 10 أيام)  
> **تاريخ البدء**: 21 ديسمبر 2024

---

## 🚨 أخطاء عاجلة (يجب إصلاحها أولاً)

### 1. [ ] بعض الصور لا تظهر في الصفحة الرئيسية
- **الوصف**: صور المنافسات/المتنافسين لا تظهر أحياناً
- **التحقق من**: lazy loading، مسارات الصور، onerror handlers، CDN

### 2. [ ] صورة البروفايل ثابتة بعد تسجيل الدخول
- **الوصف**: تظهر صورة avatar افتراضية بدلاً من الصورة الحقيقية من DB
- **التحقق من**: استرجاع avatar_url من API، تحديث State بعد login

### 3. [ ] زر "تحميل المزيد" لا يعمل في صفحة البحث
- **الوصف**: لا يحمل المزيد من المستخدمين أو المنافسات
- **الحل**: تنفيذ pagination صحيح مع offset/limit

### 4. [ ] تصميم كارت المستخدم غير مرضي
- **الوصف**: الشكل والبيانات المعروضة تحتاج تحسين
- **المطلوب**: إعادة تصميم الكارت ليشمل معلومات أفضل

---

## 🎬 تحسينات صفحة البث المباشر (لاحقاً)

### تصميم الصفحة:
- [ ] إضافة Header و Footer الخاصين بالمنصة
- [ ] نظام تعليقات يظهر على الفيديو بخلفية شفافة (مثل TikTok)
- [ ] صورة صغيرة للمتنافس الواحد من البث الخاص به
- [ ] صورة كبيرة للمتنافس الآخر (مثل Messenger)
- [ ] للمشاهدين: عرض المتنافسين جنباً لجنب

---

## ⏱️ جدول العمل (10 أيام)

| اليوم | المهام |
|-------|--------|
| **1-2** | إصلاح الأخطاء العاجلة (#1-#4) |
| **3-4** | نظام التقييم + حالة الانشغال |
| **5-6** | توحيد كروت المنافسات + i18n + Dark Mode |
| **7-8** | نظام التوصيات + صفحات إضافية |
| **9-10** | الاختبار النهائي + التجهيز للإطلاق |

---

## ✅ مهام مكتملة
- [x] إصلاح ظهور المستخدمين في صفحة البحث
- [x] إضافة معلومات المستخدم للـ API (followers, is_busy, etc.)
- [x] إصلاح Dark Mode لحالة "لا يوجد منافسات"
- [x] نقل app.js للـ head مع defer
- [x] إضافة مفاتيح i18n: search_users_prompt

---

## ⚡ المحاذير والمخاطر الرئيسية

### 1. مشاركة متنافس في أكثر من منافسة
- حقل `is_busy` و `current_competition_id` في جدول users
- عند ضغط زر البث → الحالة "مشغول"
- لا تحرير إلا بإنهاء البث أو انتهاء المنافسة

### 2. هجر المتنافس لمنافساته
- الفورية: ساعة واحدة من الإنشاء
- المجدولة: ساعة من وقت البدء المحدد
- بعد انتهاء الوقت → حذف تلقائي + إشعار

### 3. المنافسة الحية لا تتجاوز ساعتين
- دالة مؤقتة تنهي المنافسة تلقائياً
- تحويل لمسجلة + اعتذار للمشاهدين

### 4. تضارب الطلبات
- عند قبول أي طلب → حذف كل الطلبات والدعوات الأخرى

---

## 🔧 إصلاحات تقنية

### نظام التقييم (Rating System)
- كل منافسة: مجموع نقاط الطرفين = 100
- نجمة واحدة من مشاهد = 20 نقطة
- 5 نجوم = 100 نقطة
- متوسط التقييم = مجموع تقييماته ÷ عدد منافساته

### توحيد كروت المنافسات
- استخراج component موحد `CompetitionCard`
- استخدامه في: الرئيسية، البحث، منافساتي، البروفايل

### إصلاح i18n
- استبدال النصوص الإنجليزية الثابتة بمفاتيح
- إضافة مفاتيح الترجمة المفقودة

### إصلاح Dark Mode
- مراجعة كل العناصر لضمان dual styling
- إضافة dark: classes المفقودة

---

## 🎯 نظام التوصيات

### المعايير (10 معايير)
1. اللغة + البلد
2. الأحدث
3. الأعلى مشاهدة
4. الأعلى تقييماً
5. منافسات المتابَعين
6. الأقسام المفضلة
7. المشابهة لما أُعجب به
8. الكلمات المفتاحية
9. التشابه بالأقسام
10. عشوائي للتنوع

---

## 🛡️ حماية البث والسيرفر

1. **طابور إعادة المحاولة (Retry Queue)**: 3 محاولات
2. **تنظيف الملفات الفوري**: بعد انتهاء البث
3. **التحقق من المصدر**: من domain المنصة فقط
4. **Rate Limiting**: للحماية من DDoS

---

**ملاحظة**: يتم تنفيذ كل نقطة حسب توجيهاتك.
