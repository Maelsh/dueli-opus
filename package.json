{
  "name": "dueli",
  "version": "1.0.0",
  "description": "منصة المنافسات والحوارات - Dueli Competition & Dialogue Platform",
  "type": "module",
  "scripts": {
    "dev": "npm run build:css && npm run build:client && vite",
    "dev:sandbox": "npm run build:css && npm run build:client && wrangler pages dev dist --d1=dueli-db --local --ip 0.0.0.0 --port 5173",
    "build": "npm run build:css && npm run build:client && vite build",
    "build:client": "npx esbuild src/client/index.ts --bundle --outfile=dist/static/app.js --format=iife --minify --sourcemap",
    "build:css": "npx @tailwindcss/cli -i ./src/styles.css -o ./public/static/styles.css",
    "preview": "wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist",
    "deploy:prod": "npm run build && wrangler pages deploy dist --project-name dueli",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "db:migrate:local": "wrangler d1 migrations apply dueli-db --local",
    "db:seed": "wrangler d1 execute dueli-db --local --file=./db/seed.sql",
    "db:reset": "node dev-tools/reset-d1.mjs && npm run db:migrate:local && npm run db:seed",
    "routes:inventory": "node dev-tools/route-inventory.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@hono/vite-cloudflare-pages": "^0.4.2",
    "hono": "^4.13.7",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260905.1",
    "autoprefixer": "^10.4.22",
    "marked": "^17.0.1",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.17",
    "typescript": "^5.0.0",
    "vite": "^7.2.6",
    "vitest": "^5.0.0",
    "wrangler": "^4.129.0"
  },
  "keywords": [
    "dueli",
    "competition",
    "dialogue",
    "platform",
    "debate"
  ],
  "author": "Maelsh Pro",
  "license": "MIT"
}
