# ⚠️ تحذيرات صارمة للمطورين - Mandatory Developer Guidelines

> [!CAUTION]
> **يُمنع منعاً باتاً بدء أي عمل تطويري قبل قراءة وفهم هذا المستند بالكامل.**
> **أي مخالفة لهذه المبادئ ستؤدي إلى رفض الكود فوراً.**

---

## 🚨 قبل أن تكتب أي كود

### ✋ توقف واقرأ!

هذا المشروع مبني على **خمسة مبادئ أساسية** لا يمكن التنازل عنها. أي كود يخالف هذه المبادئ سيتم رفضه ويجب إعادة كتابته.

---

## 📜 المبادئ الأربعة الحاكمة

### 1️⃣ دعم اللغات العالمي (Universal Language Support)

> [!IMPORTANT]
> **كل نص يظهر للمستخدم يجب أن يكون عبر i18n - بدون استثناء!**

#### ✅ صحيح
```typescript
const message = t('competition_created', lang);
const button = `<button>${tr.submit}</button>`;
```

#### ❌ خطأ - مرفوض!
```typescript
const message = 'Competition created successfully';
const button = '<button>Submit</button>';
```

#### القواعد:
- لا نصوص إنجليزية/عربية مباشرة في الكود
- كل نص جديد يُضاف إلى `src/i18n/en.ts` و `src/i18n/ar.ts`
- استخدم `t()` أو `tr.key` دائماً

---

### 2️⃣ البرمجة الكائنية (OOP)

> [!IMPORTANT]
> **كل منطق يجب أن يكون في classes مع مسؤولية واحدة!**

#### ✅ صحيح
```typescript
class CompetitionService {
  async create(data: CreateCompetitionData): Promise<Competition> {
    // منطق الإنشاء
  }
}

class CompetitionRepository extends BaseRepository<Competition> {
  async findByCreator(userId: number): Promise<Competition[]> {
    // الوصول للبيانات
  }
}
```

#### ❌ خطأ - مرفوض!
```typescript
// دوال منفصلة بدون تنظيم
async function createCompetition(data) { }
async function getCompetitions() { }
```

#### القواعد:
- **Class per responsibility** - كل class لمسؤولية واحدة
- **Inheritance** - استخدم الوراثة (extends BaseController, extends BaseModel)
- **Encapsulation** - private/public للخصائص والدوال
- **لا تكرار** - DRY principle

---

### 3️⃣ هيكلة MVC (Model-View-Controller)

> [!IMPORTANT]
> **فصل تام بين البيانات والعرض والتحكم!**

```
┌─────────────────────────────────────────────┐
│                   REQUEST                    │
└─────────────────────┬───────────────────────┘
                      ▼
┌─────────────────────────────────────────────┐
│             CONTROLLER                       │
│  - يستقبل الطلب                              │
│  - يتحقق من الصلاحيات                        │
│  - يستدعي Model/Service                     │
│  - يرسل الرد                                 │
└─────────────────────┬───────────────────────┘
                      ▼
┌─────────────────────────────────────────────┐
│               MODEL                          │
│  - الوصول لقاعدة البيانات                    │
│  - منطق البيانات                             │
│  - لا يعرف شيء عن HTTP                       │
└─────────────────────┬───────────────────────┘
                      ▼
┌─────────────────────────────────────────────┐
│                VIEW                          │
│  - HTML/Template فقط                        │
│  - لا منطق عمل                              │
│  - مصدر واحد (shared/components)            │
└─────────────────────────────────────────────┘
```

#### ⚠️ قاعدة ذهبية: مصدر واحد للـ View!
```
src/shared/components/competition-card.ts    ← المصدر الوحيد!
       ↓
يُستخدم في: Server SSR + Client Bundle
       ↓
لا نسخ! لا تكرار! لا كود HTML منسوخ!
```

#### ❌ خطأ فادح:
```typescript
// في ملف pages/my-competitions-page.ts
function renderCard(item) {
  return `<div class="card">...</div>`; // ❌ تكرار!
}

// في ملف pages/explore-page.ts
function renderCard(item) {
  return `<div class="card">...</div>`; // ❌ تكرار!
}
```

#### ✅ صحيح:
```typescript
// مصدر واحد في shared/components/competition-card.ts
export function getCompetitionCard(item, lang) { ... }

// في أي صفحة
window.renderCompetitionCard(item, lang);
```

---

### 4️⃣ إمكانية الوصول (Accessibility)

> [!IMPORTANT]
> **المشروع يدعم المكفوفين وضعاف البصر - هذا ليس اختيارياً!**

#### كل عنصر تفاعلي يجب أن يحتوي:

| العنصر | المطلوب |
|--------|---------|
| `<label>` | `for="inputId"` |
| `<button>` | `title="${tr.key}"` + `aria-label` إذا icon-only |
| `<img>` | `alt` وصفي (ليس فارغ إلا للزخرفة) |
| `<dialog>` | `role="dialog"` + `aria-modal="true"` |
| `<i class="icon">` | `aria-hidden="true"` + `<span class="sr-only">` |

#### ✅ صحيح
```html
<label for="emailInput">${tr.email}</label>
<input type="email" id="emailInput" title="${tr.enter_email}">

<button title="${tr.close}" aria-label="${tr.close}">
  <i class="fas fa-times" aria-hidden="true"></i>
  <span class="sr-only">${tr.close}</span>
</button>
```

#### ❌ خطأ - مرفوض!
```html
<label>Email</label>
<input type="email">

<button>
  <i class="fas fa-times"></i>
</button>
```

---

### 5️⃣ التنسيق المزدوج (Dark/Light Mode)

> [!IMPORTANT]
> **كل عنصر يجب أن يدعم الوضع الليلي والنهاري - بدون استثناء!**

#### ✅ صحيح
```html
<!-- الخلفيات -->
<div class="bg-gray-100 dark:bg-gray-800">

<!-- النصوص -->
<p class="text-gray-700 dark:text-gray-300">

<!-- الحدود -->
<div class="border border-gray-200 dark:border-gray-700">

<!-- التأثيرات -->
<button class="hover:bg-gray-200 dark:hover:bg-gray-700">
```

#### ❌ خطأ - مرفوض!
```html
<!-- بدون دعم الوضع الليلي -->
<div class="bg-white">
<p class="text-gray-600">
<div class="border-gray-200">
```

#### القواعد:
- **كل `bg-*`** يجب أن يرافقه `dark:bg-*`
- **كل `text-*`** يجب أن يرافقه `dark:text-*`
- **كل `border-*`** يجب أن يرافقه `dark:border-*`
- **الاستثناء الوحيد**: العناصر الثابتة (مثل الشعارات الملونة)

---

## 🛑 المحظورات المطلقة

> [!CAUTION]
> مخالفة أي من هذه = رفض الكود فوراً!

### ممنوع منعاً باتاً:

| # | المحظور | البديل |
|---|---------|--------|
| 1 | نسخ الكود من ملف لآخر | إنشاء مكون مشترك |
| 2 | نصوص إنجليزية/عربية مباشرة | مفاتيح i18n |
| 3 | `any` type في TypeScript | تعريف Interface مناسب |
| 4 | `console.log` في الكود النهائي | حذفها قبل الـ PR |
| 5 | منطق عمل في الـ View | نقله للـ Controller/Service |
| 6 | SQL queries في Controllers | نقلها للـ Model |
| 7 | عناصر تفاعلية بدون title | إضافة title + aria-label |
| 8 | labels بدون for attribute | ربطها بـ input id |

---

## 📋 قائمة التحقق قبل الـ Pull Request

### التحقق الإلزامي:

- [ ] **i18n**: كل النصوص عبر مفاتيح الترجمة
- [ ] **OOP**: الكود منظم في classes
- [ ] **MVC**: فصل واضح بين Model/View/Controller
- [ ] **Accessibility**: 
  - [ ] كل label لها for
  - [ ] كل button لها title
  - [ ] كل img لها alt
  - [ ] الأيقونات لها sr-only
- [ ] **DRY**: لا كود مكرر
- [ ] **Types**: لا `any` types
- [ ] **Clean**: لا `console.log` statements
- [ ] **Build**: `npm run build` ناجح بدون أخطاء

---

## 🏗️ هيكل المشروع المعتمد

```
src/
├── config/           # الإعدادات والأنواع
├── controllers/      # Controllers - التحكم بالـ HTTP
├── models/           # Models - الوصول للبيانات
├── lib/services/     # Services - منطق العمل المعقد
├── modules/          
│   ├── api/          # API routes
│   └── pages/        # SSR pages (Controllers)
├── shared/
│   ├── components/   # 🔴 VIEW - المكونات المشتركة
│   └── templates/    # قوالب HTML
├── client/
│   ├── core/         # State, ApiClient
│   ├── services/     # Auth, Theme services
│   ├── ui/           # Toast, Modal, Menu
│   └── helpers/      # أدوات مساعدة
└── i18n/             # ملفات الترجمة
```

### قاعدة ذهبية للـ Views:
```
✅ shared/components/    ← كل الـ Views هنا
❌ pages/*.ts            ← لا HTML مكرر هنا
```

---

## ⚡ الإجراء عند اكتشاف مخالفة

1. **الكود لن يُدمج** حتى إصلاح المخالفة
2. يجب إعادة الكتابة وفق المبادئ
3. مراجعة المستند مرة أخرى
4. فتح PR جديد

---

## 📚 القراءة الإجبارية

قبل أي عمل، اقرأ أيضاً:
- [Code Violations Report](Code_Violations_Report.md) - تقرير المخالفات الحالية
- [Architecture](Architecture.md) - الهيكلة العامة
- [API Reference](API_Reference.md) - مرجع الـ APIs

---

> [!WARNING]
> **تذكر**: هذه المبادئ ليست اقتراحات - إنها متطلبات إلزامية.
> 
> الهدف هو كود نظيف، قابل للصيانة، ويدعم الجميع بما فيهم المكفوفين.
> 
> **اقرأ. افهم. التزم.**

---

*آخر تحديث: 2025-12-18*
